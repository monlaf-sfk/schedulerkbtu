
# KBTU Schedule Builder

**KBTU Schedule Builder** — это веб-приложение, разработанное для студентов Казахстанско-Британского Технического Университета (КБТУ), которое помогает составлять, анализировать и оптимизировать учебное расписание.

Приложение позволяет автоматически загружать данные о курсах с портала WSP, визуализировать расписание, выявлять конфликты по времени и кредитам, а также экспортировать готовое расписание в различные форматы.

## ✨ Основные возможности

  * **🔍 Парсинг данных:** Автоматический сбор актуальных данных о курсах, преподавателях и аудиториях с портала `wsp.kbtu.kz` с использованием Playwright.
  * **📅 Визуальный конструктор:** Интерактивная сетка расписания с поддержкой различных типов занятий (Лекции, Лабораторные, Практики).
  * **⚡ Обнаружение конфликтов:** Автоматическая подсветка пересечений занятий по времени и нарушений формулы курса (количество лекций/лабов/практик).
  * **💡 Умные рекомендации:** Система подсказок для разрешения конфликтов и подбора оптимальных секций.
  * **📂 Управление расписаниями:** Возможность создавать, дублировать и сравнивать несколько вариантов расписания одновременно.
  * **💾 Экспорт:** Поддержка экспорта расписания в форматы:
      * `iCal` (для Google Calendar, Apple Calendar)
      * `CSV` (для Excel)
      * `JSON`
      * Текстовый формат
  * **🎨 Темная тема:** Современный UI, выполненный в темных тонах с использованием Tailwind CSS.

## 🛠 Технологический стек

### Frontend

  * **React** (Vite) — основной фреймворк.
  * **TypeScript** — типизация.
  * **Tailwind CSS** — стилизация.
  * **Radix UI** — доступные компоненты (модальные окна).
  * **React Router** — навигация.
  * **Lucide React** — иконки.

### Backend

  * **Node.js & Express** — серверная часть.
  * **Prisma ORM** — работа с базой данных.
  * **SQLite** — база данных для хранения курсов и секций.
  * **Playwright** — скрапинг данных с WSP.

## 🚀 Установка и запуск

### Предварительные требования

  * Node.js (версия 18 или выше)
  * npm или yarn

### 1\. Клонирование репозитория

```bash
git clone https://github.com/monlaf-sfk/schedulerkbtu.git
cd schedulerkbtu
```

### 2\. Настройка Бэкенда (Backend)

Перейдите в папку backend:

```bash
cd backend
```

Установите зависимости:

```bash
npm install
```

Настройте переменные окружения. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

**Важно:** Для работы парсера необходимо указать логин и пароль от WSP в файле `.env`:

```env
PORT=5001
DATABASE_URL="file:./prisma/dev.db"
LOGIN="ваш_логин_wsp"
PASSWORD="ваш_пароль_wsp"
```

Установите браузеры для Playwright (необходимо для парсинга):

```bash
npx playwright install chromium
```

Выполните миграции базы данных и запустите сервер:

```bash
npx prisma migrate dev --name init
npm run dev
```

*Сервер запустится на порту 5001.*

### 3\. Настройка Фронтенда (Frontend)

Откройте новый терминал и перейдите в папку frontend:

```bash
cd frontend
```

Установите зависимости:

```bash
npm install
```

Настройте переменные окружения. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Убедитесь, что `VITE_API_URL` указывает на ваш бэкенд:

```env
VITE_API_URL=http://localhost:5001
```

Запустите клиентскую часть:

```bash
npm run dev
```

*Приложение будет доступно по адресу http://localhost:5173 (или другой порт, указанный в консоли).*

## 📖 Использование

1.  **Добавление курсов:**
      * Нажмите кнопку **"+ Добавить курсы"** в боковом меню.
      * Введите код курса (например, `CSCI1201`). Если курса нет в локальной базе, нажмите кнопку "Найти и добавить", чтобы запустить парсер.
2.  **Составление расписания:**
      * Выбирайте секции (лекции, практики, лабы) в боковой панели или на сетке.
      * Следите за индикаторами конфликтов и прогресс-барами выполнения требований курса.
3.  **Анализ и Сравнение:**
      * Используйте вкладку **"Анализ"** для проверки выполнения требований по кредитам.
      * Создавайте копии расписаний и используйте функцию сравнения, чтобы выбрать лучший вариант.
4.  **Экспорт:**
      * Нажмите кнопку экспорта рядом с названием расписания, чтобы скачать его в нужном формате.

## 📂 Структура проекта

```text
schedulerkbtu/
├── backend/
│   ├── prisma/            # Схемы БД и миграции
│   ├── src/
│   │   ├── controllers/   # Логика обработки запросов
│   │   ├── routes/        # API маршруты
│   │   ├── services/      # Бизнес-логика (scraperService.ts)
│   │   └── index.ts       # Точка входа
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── components/    # React компоненты (Analysis, Course, Schedule, UI)
│   │   ├── hooks/         # Кастомные хуки (useScheduleManager, etc.)
│   │   ├── types/         # TypeScript интерфейсы
│   │   ├── utils/         # Вспомогательные функции
│   │   └── App.tsx
│   └── package.json
└── README.md
```

## 🤝 Вклад в проект (Contributing)

Если вы хотите улучшить проект:

1.  Форкните репозиторий.
2.  Создайте ветку для вашей фичи (`git checkout -b feature/AmazingFeature`).
3.  Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`).
4.  Запушьте ветку (`git push origin feature/AmazingFeature`).
5.  Откройте Pull Request.

## 📄 Лицензия

Проект распространяется под лицензией ISC.

-----

*Made with ♥ by students for students.*
