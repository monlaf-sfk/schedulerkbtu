# Backend environment example
# Copy this file to .env and change values for your server

# Server binding
PORT=5001
# HOST to bind the Express server. Use 0.0.0.0 to accept external connections.
HOST=0.0.0.0

# Optional: public-facing host or URL used only for logs or building client URLs
# Set API_HOST to a hostname (e.g. api.example.com) or PUBLIC_URL to a full URL.
# If both are empty, the server will log http://localhost:PORT
#API_HOST=api.example.com
PUBLIC_URL=https://api.example.com

# Database (example for sqlite)
# For SQLite (file relative to project root):
DATABASE_URL="file:./prisma/dev.db"

# For PostgreSQL (uncomment and use instead):
# DATABASE_URL="postgresql://scheduler:StrongPassword123!@localhost:5432/schedulerdb?schema=public"
